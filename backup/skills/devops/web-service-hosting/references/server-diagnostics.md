Archived source skill: server-diagnostics

This reference preserves the absorbed diagnostics patterns:
- layered diagnosis order (process → port → localhost HTTP → TLS → HTTPS → proxy)
- hung-listener / backlog-recognition patterns
- stale-process vs stale-build troubleshooting
- upstream dependency reachability testing and campus/VPN-style network restrictions

Recover the full original skill and its linked references from ~/.hermes/skills/.archive if needed.