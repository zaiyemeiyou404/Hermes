Archived source skill: design-ai-deployment

This reference preserves the app-specific deployment knowledge that was too narrow for a top-level skill:
- deploying Open Design-like Docker apps
- adjusting loopback-only port bindings
- Caddy reverse-proxy and DNS bring-up
- BYOK/API-provider configuration
- JS-heavy SPA settings and localStorage injection as a fallback when browser automation fails

The original full skill lives in ~/.hermes/skills/.archive for recovery.